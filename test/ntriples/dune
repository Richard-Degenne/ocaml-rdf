(data_only_dirs data)

(executable
  (name runtest)
  (libraries alcotest ntriples)
)

(rule
  (alias runtest)
  (deps (source_tree data) runtest.exe)
  (action (run ./runtest.exe))
)
; FIXME: For some reason, dune does not rerun the test executable when data files change.
